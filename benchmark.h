#ifndef BENCHMARK_H
#define BENCHMARK_H

// Benchmark tests to decide which bigint libraries are best

class benchmark {
 public:
  benchmark();

  void test1();

  void test2();
};

#endif  // BENCHMARK_H
